name: quran_web_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # تم تعديل هذا لضمان التوافق

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0           # للاتصال بالـ API
  audioplayers: ^5.2.1   # لتشغيل الصوتيات
  google_fonts: ^6.1.0   # لخطوط عربية جميلة
  provider: ^6.0.5       # لإدارة حالة التطبيق
  quran: ^1.0.0          # مكتبة مساعدة
  
  # 👇👇👇 هذه هي المكتبة الناقصة التي كانت تسبب المشكلة 👇👇👇
  scrollable_positioned_list: ^0.3.8 

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true